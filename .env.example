# Supabase (Europe-DC-Render project) — publishable/anon key is safe for the browser (protected by RLS)
NEXT_PUBLIC_SUPABASE_URL=https://gjafpsjvufbwgjxvexjq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_publishable_or_anon_key_here

# Stripe (added in a later phase) — keep the SECRET key server-side only, never NEXT_PUBLIC
# STRIPE_SECRET_KEY=sk_live_...
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
